# scripts/

Тулинг, которым собирается пакет. Источник истины — каталог `svgs/`; всё
остальное (`fonts/`, `lib/src/**`, `assets/icon-preview/`, `example/lib/icon_registry.dart`)
из него генерируется и коммитится в репозиторий.

## Конвейер

```
cdn.iconsax.io
      │  download_icon_parts.py      каталог иконок (JSON)
      ▼
all_icons_data.json
      │  download_svgs.py            скачать + расшифровать SVG
      ▼
   svgs/                             ◀── источник истины
      │  generate_fonts_api.py       SVG → шрифты + Dart API
      ▼
fonts/ + lib/src/**
      │  fix_font_metrics.py         ⚠ обязательно
      │  fix_layer_opacities.py      ⚠ обязательно
      ▼
      │  generate_example_registry.py
      │  generate_previews.py
      ▼
   готовый пакет
```

Два шага `fix_*` — не опциональные: генератор оставляет после себя два дефекта,
описанных ниже. Оба скрипта идемпотентны и понимают `--check` (проверить без
записи, ненулевой код возврата при находках).

## Основные

### `download_icon_parts.py`
Тянет `icons_part_1..10.json` с `cdn.iconsax.io/icons_parts/` и склеивает в один
`all_icons_data.json`. Это каталог: для каждой иконки — `style`, `category`,
`tier` (`free`/`pro`) и путь на CDN. Нужен дальше не только для скачивания, но и
для разрешения коллизий имён (см. ниже).

### `download_svgs.py`
По каталогу скачивает все `tier == free` иконки. Файлы на CDN зашифрованы
AES-256-CBC в формате OpenSSL (`Salted__` + пароль `123qwe`), скрипт их
расшифровывает и раскладывает в `svgs/<style>/<category>/<имя>_<ключевые-слова>.svg`
(имя файла с CDN как есть; у части файлов хвост `-temp`). 100 потоков, 3 попытки
на файл.

> **Затирает `svgs/` целиком** (`shutil.rmtree`) перед стартом.

### `generate_fonts_api.py`
Главный генератор. Бьёт SVG на батчи (по 4 для `twotone`, иначе по 50), шлёт на
`app.iconsax.io/api/svg-to-code`, при ошибке рекурсивно делит батч пополам.
Полученные TTF чистит от `GSUB`/`GPOS`/`GDEF`, сливает через `fontTools.merge` и
на выходе пишет:

- `fonts/iconsax_<style>.{ttf,woff,woff2}`
- `lib/src/static/<style>.dart` — «сырые» `IconData` по одному на глиф
- `lib/src/iconsax_<style>.dart` — публичный класс; для `bulk`/`twotone` это
  композитные `IconsaxIconData` со списком слоёв
- `lib/src/static/iconsax_data.dart`, `lib/src/static/iconsax_resolver.dart`

Имена: `имя-файла-до-первого-подчёркивания` → camelCase. Если внутри стиля
короткое имя встречается больше одного раза, добавляется суффикс — последние 4
hex-символа md5 от CDN-URL иконки (поэтому и нужен `all_icons_data.json`).

## Обязательные правки после генерации

### `fix_font_metrics.py`
Эндпоинт отдаёт **правильные контуры**, но пишет `hmtx.leftSideBearing = 0`
каждому глифу, хотя реальный `xMin` доходит до 188 из 200 юнитов em.
Растеризатор двигает глиф так, чтобы `xMin` совпал с `lsb`, — и иконка уезжает
влево: 1–2 px при размере 24 у однотонных стилей, 6–22 px у каждого слоя
`bulk`/`twotone` (слои при этом ещё и расходятся между собой).

Что делает:
1. `lsb = xMin` для всех глифов — это и есть основная правка, контуры не трогаются;
2. полный em в advance width;
3. центрирует 9 иконок, у которых исходный viewBox не 24×24 (эндпоинт прижимает
   их в угол em);
4. обнуляет `typoLineGap` (было 18/200 — смещало многослойные иконки вниз на ~9%)
   и пересчитывает производные поля `head`/`hhea`/`OS/2`;
5. перезаписывает `.ttf`/`.woff`/`.woff2`.

### `fix_layer_opacities.py`
Генератор строит два списка `IconsaxIconData` в **разном порядке** и спаривает их
позиционно: `icons` отсортирован по кодпоинту, `opacities` — в порядке документа
SVG. Где порядки расходятся, тень 0.4 попадает не на тот слой. Плюс
`<g opacity="0.4">` разворачивается в дочерние пути, и собственная прозрачность
группы теряется.

Скрипт перечитывает слои из SVG: спускается сквозь обёрточные `<g clip-path>` (у
них нет своего `opacity`), а на нужном уровне каждый `<path>` = `pathN`, каждый
`<g>` = `groupN` — ровно те имена, что даёт эндпоинт. Слои выдаются в порядке
документа, который заодно является правильным порядком отрисовки (первый —
нижний), каждый со своей прозрачностью. Набор слоёв у иконки не меняется —
только порядок и прозрачности.

Нужен `all_icons_data.json` (`--icons-json`), чтобы воспроизвести md5-суффиксы
коллизионных имён.

## Постобработка

### `generate_example_registry.py`
Собирает `example/lib/icon_registry.dart` — `Map<String, IconData>` /
`Map<String, IconsaxIconData>` на стиль, чтобы галерея в примере могла
перечислить иконки по именам.

### `generate_previews.py`
Рендерит PNG-превью в `assets/icon-preview/<style>/<name>.png` и подставляет
ссылку `/// ![](...)` в docstring каждого геттера.

> Отстал от текущего состояния: в `main()` захардкожены только `bold` и
> `outline` (для остальных стилей превью не сгенерированы), фильтр рамки
> `1000×1000` рассчитан на em 1000, а сейчас em 200, прозрачность twotone
> вешается на «нулевой контур» вместо реального слоя. Рендер идёт своим
> полигональным растеризатором, а не через FreeType.

## Диагностика и разовые проверки

| Скрипт | Что показывает |
|---|---|
| `check_duplicates.py` | по стилям: сколько уникальных коротких имён, файлов, и сколько коллизий с **разным** содержимым |
| `count_free_icons.py` | статистика по `all_icons_data.json`: сколько free-иконок, разбивка по стилям/term/tier |
| `count_resolver_keys.py` | сколько кодпоинтов на стиль в резолвере |
| `test_suffix_generation.py` | печатает примеры md5-суффиксов для коллизионных имён twotone |
| `test_opacity_mapping.py` | сверяет число фигур в SVG с числом геттеров в Dart (twotone) |
| `find_star_svgs.py` | разовый листинг `svgs/twotone/**/star*` |

Последние три — черновые проверки времён отладки, не тесты. `count_resolver_keys.py`
и `test_opacity_mapping.py` смотрят на старые пути (`lib/iconsax_resolver.dart`,
`lib/src/twotone.dart`) — файлы переехали в `lib/src/static/`, так что без правки
пути они упадут.

## Устаревшее

### `split_fonts.py`
Ранняя схема: один общий `fonts/iconsax.ttf` резался на 6 подшрифтов через
`pyftsubset` по кодпоинтам из резолвера, плюс снятие рамок 1000×1000. Сейчас
`generate_fonts_api.py` отдаёт пошрифтовую нарезку сразу, а `fonts/iconsax.ttf`
в репозитории нет — скрипт нерабочий, оставлен как история.

## Оговорки

- **Пути захардкожены.** Почти во всех старых скриптах прописан
  `base_dir = "/Users/aidarmaratbekov/Developer/iconsax"`. Только `fix_font_metrics.py`
  и `fix_layer_opacities.py` вычисляют корень от своего расположения и принимают
  аргументы.
- **`all_icons_data.json` лежит в скретче Gemini** (`~/.gemini/antigravity/brain/<uuid>/scratch/`),
  а не в репозитории. Каталог временный: если он пропадёт, коллизионные имена
  восстановить будет нечем — файл стоит куда-нибудь скопировать.
- **`scripts/` в `.gitignore`.** В индексе только `fix_font_metrics.py`,
  `fix_layer_opacities.py`, `generate_previews.py`, `split_fonts.py` (добавлены
  через `git add -f`). Остальное живёт только локально.
- **Зависимости:** `fonttools`, `brotli` (шрифты), `pillow` (превью),
  `cryptography` (скачивание SVG).

## Известные проблемы, которые скрипты не чинят

При слиянии батчей в `generate_fonts_api.py` часть глифов теряется из-за
коллизий кодпоинтов и имён:

- **66 иконок** есть в `svgs/`, но недостижимы в шрифтах (`bold/heart`,
  `linear/blur`, `outline/eye`, `bulk/call` и др.);
- **755 слоёв в `bulk` и 475 в `twotone`** есть в SVG, но их глифов нет в шрифте,
  поэтому иконки рисуются неполными (например, `bulk/archive-minus` — только
  `path2`, `bulk/save-minus` — только `path3`).

Лечится только регенерацией с починенной раздачей кодпоинтов, а это меняет
кодпоинты, то есть breaking change для пользователей пакета.
